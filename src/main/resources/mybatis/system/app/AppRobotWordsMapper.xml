<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.superhao.app.mapper.AppRobotWordsMapper">

	<!-- 可根据自己的需求，是否要使用 -->
    <resultMap type="com.superhao.app.entity.AppRobotWords" id="appRobotWordsMap">
        <result property="wordsId" column="words_id"/>
        <result property="wkey" column="wkey"/>
        <result property="wvalue" column="wvalue"/>
        <result property="dictionaryId" column="dictionary_id"/>
    </resultMap>

    <sql id="baseSelecet">
                words_id,
                wkey,
                wvalue,
                dictionary_id,
            </sql>

</mapper>